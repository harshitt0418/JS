//if

if(/*condition->*/true){  
}

const isUserLoggedIn=true
if(isUserLoggedIn){
}

//conditional operators in js
//< , > , <= , >= , == , != , === , !== //=== -> it also checks the datatypes if two values are same but their datatype are different it will return false

if( 2=="2" ){
    console.log("executed");
}

if( 2==="2" ){//it wont execute at it is not true
    console.log("executed");
}

//if(condition) console.log(hello), console.log(world);//if else can be written like this also but this a bad practice so avoid it!!

